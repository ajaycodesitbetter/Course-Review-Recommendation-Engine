Place your branding images here.

Required filenames (replace these with your actual files):
- coursescout-icon.png        # square icon (e.g., your smiley)
- coursescout-wordmark.png    # the CourseScout text image (wide wordmark)

After adding the files, just refresh the site. The code will gracefully
fall back to the old icon/text if these images are missing or fail to load.
